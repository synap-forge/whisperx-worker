# ---------- PyTorch CUDA 12.4 wheels ----------
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.4.0
torchaudio==2.4.0

# ---------- main libs ----------
whisperx
pyannote.audio               # embedded/diarization
speechbrain                 # ECAPA embedding that whisperx uses
ctranslate2>=4.0.0           # 4.x targets CUDA 12, includes sm_89/sm_90 kernels
# ---------- speechbrain dependencies ----------
sentencepiece
# ---------- utilities ----------
pydub
numpy<2                      # np.NaN removed in NumPy 2.0, pyannote.audio requires <2
scipy
librosa
huggingface-hub
runpod
cog
speechbrain
cryptography
python-dotenv
# ---------- runpod / cog ----------
# Latest runpod (s2.x) supports Python 3.10 / 3.11
runpod
# "cog" is **only** needed in local dev; drop it in container
